/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { RomanPipe } from './roman.pipe';

describe('RomanPipe', () => {
  it('create an instance', () => {
    let pipe = new RomanPipe();
    expect(pipe).toBeTruthy();
  });
});
